- [x] Phase 4: `report_writer_agent.py`에서 추론 모델의 무한 루프(Token Exhaustion)를 유발하던 원시 텔레메트리(`context_bundle`) 주입 로직 제거 완료.
- [x] Dev1 스택 기동 실패 진단 (2026-09-07):
  - 원상: `deeptempo` 스택이 6379 포트를 점유 중 → AiSOC redis 바인드가 충돌. 루트 `docker-compose.yml`의 모든 공개 포트를 `HOST_*_PORT` 변수로 파라미터화 (`${VAR:-default}` 형태).
  - Kafka 기동 실패 원상: ZooKeeper 모드의 브로커는 클러스터 ID를 ZK에서 읽으며, `KAFKA_CLUSTER_ID` 환경변수는 ZK 모드에서 무효. `aisoc-dev1_kafka_data` 볼륨의 `meta.properties`에 저장된 `cluster.id`를 ZK의 현재 ID(`8fEwK36GS8Cpy1CCInNOHg`)로 정합화 완료 → 브로커 기동 성공 (healthy).
  - `HOST_LITELLM_PORT` 키를 `.env`(dev1)에 추가. `.env.example`에 `HOST_*` 포트 바인딩 가이드 블록 추가 후, 비효력 `KAFKA_CLUSTER_ID` 주석은 제거.
  - `docker compose -f docker-compose.yml --env-file .env up -d` 전체 스택 기동 확인 완료.
- [x] `services/ingest/internal/normalizer/normalizer.go` duplicate `"splunk"` map key 정리 (2026-09-07):
  - 원상: `connectorProfiles`에 `"splunk"` 키가 3개 정의 (4001 raw-fields 프로필 + 바이트 동일 2001 canonical 블록 2개). Go는 마지막 정의가 이기므로 유효한 항목은 2001 canonical 프로필.
  - 조치: 4001 raw-fields 프로필 삭제, 바이트 동일 2001 블록 중 1개 제거 → 단일 `"splunk"` 엔트리(2001)로 통합. `go vet` / `go build -buildvcs=false` / `go test` (golang:1.25 컨테이너에서 실행) 전부 통과.
  - 커밋 `7a6d5695`로 푸시 완료 (포트 파라미터화 + splunk 통합 포함).
  - 이후 사용자 요청으로 리버트(`7764d852`) → 원복. 다시 단일 통합으로 재적용:
    - 단일 `"splunk"` 엔트리: raw Splunk 필드(22개) + canonical 키(4개)를 하나의 `fieldMap`에 병합, severityMap은 대문자 포함 10개 퍼셋 유지. classUID 2001.
    - `go vet` / `go build -buildvcs=false` / `go test` 전부 통과 (golang:1.25 컨테이너).
- [x] 포스트 리버트 후 5432 포트 충돌 재발생 대응 (2026-09-07):
  - 리버트(`7764d852`)로 루트 `docker-compose.yml` 포트 바인딩이 하드코딩으로 복귀 → `deeptempo-postgres`(0.0.0.0:5432)와 충돌 재발생.
  - 대응: 30개 `HOST_*_PORT` 변수로 모든 공개 포트를 재파라미터화 + web `NEXT_PUBLIC_*`를 `.env`에서 참조하도록 수정.
  - Kafka ZK 클러스터 ID가 또 재초기화되어 새 ID(`1KoCPb_ASsO91ISjjqs66g`) 발급 → 볼륨 `meta.properties`를 정합화 후 kafka healthy.
  - `docker compose --env-file .env up -d` 전체 스택 기동 확인: 모든 서비스가 dev1 포트(1xxxx)로 바인드, `api /health` → 200.
